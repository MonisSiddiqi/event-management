import { RefreshCcwIcon } from "lucide-react";

export const GlobalPending = () => {
  return (
    <div className="flex flex-1 flex-col items-center justify-center">
      <RefreshCcwIcon className="h-10 w-10 animate-spin" />
    </div>
  );
};
